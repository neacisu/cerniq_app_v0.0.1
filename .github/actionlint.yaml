# https://github.com/rhysd/actionlint/blob/main/docs/config.md
# Validează variabile și secrete GitHub referite în workflow-uri (vars.* / secrets.*).
self-hosted-runner:
  labels:
    - self-hosted

config-variables:
  - SONAR_SCAN
  - CERNIQ_OPENBAO_SONAR_ENV_FILE

config-secrets:
  - SONAR_TOKEN
