/**
 * Shim IDE / diag — implementarea canonică: `apps/api/src/errors/handler.ts`.
 * O singură linie de re-export evită duplicarea logicii și păstrează același tip ca sursa.
 */
export { errorHandler } from "../../apps/api/src/errors/handler.js";
