/**
 * Bridge pentru IDE / analizori care rezolvă `@/components/**` din stratul `src/**`
 * al monorepo-ului în locul sursei canonice din `apps/web/src/**`.
 * Sursa canonică: `apps/web/src/components/etapa1/PipelineProgressPanel.tsx`.
 */
export { PipelineProgressPanel } from "./PipelineProgressPanel.tsx";
