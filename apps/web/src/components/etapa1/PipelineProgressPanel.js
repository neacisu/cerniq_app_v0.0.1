/**
 * Bridge pentru importuri cu sufix `.js` (rezoluție TypeScript / Vite).
 * Sursa canonică: `./PipelineProgressPanel.tsx`.
 */
export { PipelineProgressPanel } from "./PipelineProgressPanel.tsx";
