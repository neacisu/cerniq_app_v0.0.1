{
  "name": "cerniq-app",
  "version": "0.0.1",
  "private": true,
  "description": "Cerniq.app - B2B Sales Automation Platform",
  "scripts": {
    "lint": "echo 'Lint placeholder - F0.6 pending'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ci": "vitest run --reporter=json --reporter=verbose --outputFile=test-results/results.json",
    "build": "echo 'Build placeholder - F0.6 pending'"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "yaml": "^2.6.1"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0"
}
