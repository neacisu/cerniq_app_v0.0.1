receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch: {}
  resource:
    attributes:
      - action: upsert
        key: project
        value: cerniq
      - action: upsert
        key: environment
        value: ${env:CERNIQ_ENV}

exporters:
  otlphttp:
    endpoint: https://otel-cerniq.neanelu.ro

# Only traces pipeline is configured for Etapa 0. Metrics and logs pipelines
# can be added when Prometheus/Vector exporters are integrated.
service:
  pipelines:
    traces:
      receivers:
        - otlp
      processors:
        - resource
        - batch
      exporters:
        - otlphttp
