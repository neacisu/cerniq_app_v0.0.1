{
  "name": "@cerniq/worker-ai",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch worker.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/worker.js",
    "lint": "echo 'No TS files yet — skeleton worker'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests yet — skeleton worker'"
  },
  "dependencies": {
    "@cerniq/db": "workspace:*",
    "@cerniq/shared-types": "workspace:*",
    "@cerniq/config": "workspace:*"
  }
}
